import styled from "styled-components";

export const Input = styled.input`
      background: inherit;
      border: none;
      color: #7F7E80;
      font-size: 16px;
      line-height: 16px;
      width: 100%;
  &:focus {
    color: #FAFAFA;
    outline: none;
  }
    `