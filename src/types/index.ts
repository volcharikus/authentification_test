export interface SelectedCountry {
    flag: string,
    dial_code: string
}

export interface UserSignUpData {
    firstName: string,
    lastName: string,
    position: string,
    phone: string
}

export interface MockedResponse {
    mocked: string,
    id: string
}